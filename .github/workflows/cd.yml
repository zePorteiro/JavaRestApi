name: "Continuous Deployment"

on:
  push:
    branches:
      - integracoes

jobs:
  deploy:
    runs-on: self-hosted 
    steps:
      - name: Pull Docker image 
        run: docker pull tiakicardozo/zeporteiro:latest

      - name: Stop Docker container 
        run: docker stop zeporteiro || true

      - name: Remove Docker container 
        run: docker rm zeporteiro || true

      - name: Run Docker container 
        run: docker run --name zeporteiro -d -p 8080:8080 tiakicardozo/JavaRestApi:latest
